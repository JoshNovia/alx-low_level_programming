#include <stdio.h>
#include <time.h>
/**
*main - Entry point
*
*Return: Always 0 (Success/Correct)
*/

int main(void)
{
char alphabet;
for (alphabet = 122 ; > = 97; alphabet--)
{
putchar(alphabet);
}

putchar( '\n' );

return (0);

}
